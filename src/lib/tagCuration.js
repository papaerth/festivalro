// ────────────────────────────────────────────────────────────────
//  태그 수동 교정 파일 — 자동 분류가 틀렸을 때 여기서 축제별로 바로잡습니다.
//
//  ▶ 축제 id 찾는 법: 그 축제 상세페이지 주소 맨 끝이 id 입니다.
//     예) chukjero.com/ko/festival/  2860107   ← 이 숫자(또는 글자)가 id
//
//  ▶ 적는 법(둘 중 편한 방식):
//     "id": { add: ["fireworks"] }              // 태그 추가
//     "id": { remove: ["water"] }               // 잘못 붙은 태그 제거
//     "id": { add: ["night"], remove: ["water"] }
//     "id": { tags: ["fireworks", "night"] }    // 자동분류 무시하고 이 태그로 통째 지정
//
//  ▶ 태그키: fireworks(🎆 불꽃놀이) · night(🌙 야간) · water(💧 물놀이)
//
//  아래 예시 줄의 // 를 지우고 실제 id/태그로 바꾸면 됩니다.
// ────────────────────────────────────────────────────────────────

export const TAG_CURATION = {
  // "2860107": { add: ["fireworks"] },      // (예시) 이 축제에 불꽃놀이 태그 추가
  // "seoulabc123": { remove: ["night"] },   // (예시) 잘못 붙은 야간 태그 제거
};
